apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy_backend.fullname" . }}
  labels:
    {{- include "deploy_backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy_backend.selectorLabels" . | nindent 4 }}
